# Core dependencies
# PyQt6  — desktop GUI only, not needed for API/web deployment
numpy
scipy
matplotlib
# sounddevice  — requires audio hardware, not needed for API deployment
soundfile
pydub
audioop-lts; python_version >= "3.13"

# Configuration
pydantic-settings
python-dotenv

# API
fastapi[standard]
python-multipart
httpx

# Auth & Payments
PyJWT
cryptography
stripe

# Testing
pytest
pytest-cov

# Optional: LLM providers (uncomment as needed)
# openai
anthropic
# tavily-python

# Dispatch flow: LangGraph multi-agent flow (diagnostics -> parts -> mechanic)
langgraph>=0.2.0
langchain-core>=0.3.0

# Repair guides (crawler + cloud DB): for scripts and when REPAIR_GUIDES_DB_URL is set
beautifulsoup4
psycopg2-binary

# PDF export for diagnosis reports
reportlab

# Web Push (optional): for mechanic job notifications
# pywebpush
